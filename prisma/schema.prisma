// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

// Note that some adapters may set a maximum length for the String type by default, please ensure your strings are long
// enough when changing adapters.
// See https://www.prisma.io/docs/orm/reference/prisma-schema-reference#string for more information
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Session {
  id            String    @id
  shop          String
  state         String
  isOnline      Boolean   @default(false)
  scope         String?
  expires       DateTime?
  accessToken   String
  userId        BigInt?
  firstName     String?
  lastName      String?
  email         String?
  accountOwner  Boolean   @default(false)
  locale        String?
  collaborator  Boolean?  @default(false)
  emailVerified Boolean?  @default(false)
  refreshToken        String?
  refreshTokenExpires DateTime?
}

model PortalUser {
  id           Int      @id @default(autoincrement())
  fullName     String
  email        String   @unique
  schoolEmail  String?
  institute    String
  role         String
  roleOther    String?
  phoneSa      String?
  passwordHash String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  customerLinks PortalUserCustomerLink[]
}

model PortalUserCustomerLink {
  id           Int        @id @default(autoincrement())
  shop         String
  customerId   String
  customerGid  String?
  portalUserId Int
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt
  portalUser   PortalUser @relation(fields: [portalUserId], references: [id], onDelete: Cascade)

  @@unique([shop, customerId])
  @@unique([shop, customerGid])
}

model StudentDiscount {
  id             Int      @id @default(autoincrement())
  shop           String
  customerId     String
  code           String
  discountNodeId String?
  shopifyDiscountId String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([shop, customerId])
}

model AutomaticDiscountRule {
  id             Int      @id @default(autoincrement())
  shop           String
  instituteKey   String
  instituteLabel String
  emailDomain    String
  categoryKey    String
  categoryLabel  String
  percentage     Int
  isActive       Boolean  @default(true)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([shop, instituteKey, categoryKey])
}

model AutomaticDiscountConfig {
  id             Int      @id @default(autoincrement())
  shop           String   @unique
  discountNodeId String?
  functionId     String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}

model BundleVisibilityRule {
  id             Int      @id @default(autoincrement())
  shop           String
  instituteKey   String
  instituteLabel String
  isEnabled      Boolean  @default(true)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([shop, instituteKey])
}
